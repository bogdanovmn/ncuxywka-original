package PsyApp::Action::404;

use strict;
use warnings;
use utf8;


sub main {
	my ($class, $params) = @_;

	return {
		#last_creos => $last_creos,
		#top_creo_list => Psy_STATISTIC::top_creo_list(psy => $psy, count => 10),
		#popular_creo_list => Psy_STATISTIC::popular_creo_list(psy => $psy, count => 10), 
	};
}

1;
